# Verification ledger

Formal verification here is a **manual discipline**: the deep checks are too
slow for CI — a since-removed model's deadlock check took ~45 minutes at
depth 5, which was the whole of it. **Since its removal the suite finishes in about
five minutes and now runs in CI on every push.** This ledger records hand-runs on
real hardware, which are still worth doing but are no longer the only thing
between a regression and nobody noticing. They run
by hand via `./formal/verify.sh`, and every full run is recorded below. Manual
verification without a ledger silently becomes *no* verification — this file is
what makes "when did we last actually run this?" answerable.

What CI does check, on every push: `quint typecheck` over all seven models, and
that every source path the models cite exists — the rot that actually happened
(eight dead citations after two crate renames) rather than the regressions that
have not.

A row here is a bounded claim at the depths documented in `formal/README.md`,
not a proof — except the six inductive rows (multihop's `noInflation`,
issuance's two supply properties, authorization's `onlyOwnerSpends`, and
reorg's two reconciliation claims), which hold at all depths. `verify.sh` enforces expected *violations* too: an attack that stops
reproducing means the model no longer models the risk it was written for.

**The subject, not the commit.** A verification result is about the `.qnt` files
and nothing else — `./scripts/subject-hash.sh formal/*.qnt`. This column held a
git commit, which cannot resolve here: the repository keeps one commit, amended
forever. If the subject hash matches, the row still describes the models in front
of you.

| Date | Subject (`.qnt` files) | Tooling | Checks | Ran by |
|---|---|---|---|---|
| 2026-07-26 | *(pre-ledger)* | quint 0.32.0 | all documented invariants, by hand during model development | posix4e + agent |
| 2026-07-28 | `e52053d3e7c108a5` | quint 0.32.0 | subject hash adopted; models unchanged since the row above | agent |
| 2026-07-28 | `dc5cbb968c6199e9` | quint 0.32.0 | 4/4 issuance rows, incl. the new `byAmount` free-mint counterexample | agent |
| 2026-07-28 | `dbbfedd795e5d8c1` | quint 0.32.0 | **FULL suite, 59/59** — every model, every documented invariant, including the new `globalSum`, `genesisUnchecked` and `genesisChecked` rows. The run reported 60 checks with 1 regression: `baserail ideal`, a **dead row** naming a module renamed to `atomic` long ago that no full run since had caught. Row removed and `baserail` re-run 9/9; `check-refs.sh` now validates module names in CI so the next one fails in seconds. | agent |
| 2026-07-28 | `c9abc6aff5213205` | quint 0.32.0 | **FULL suite, 32/32, ~5 min** — after deleting `channels.qnt` (27 checks, all of unbuilt dispute rules, and the only slow model). Every bounded `ok` row now runs at depth 8 or better; none sit at 5–6 any more. **This run is now a CI step**, not only a hand-run. | agent |
| 2026-07-28 | `186d45c720806beb` | quint 0.32.0 | **35/35, seven models** — added `authorization.qnt`: proof-native spend auth holds (`enforced`), the public-anchor strawman lets a forger steal (`anchorOnly`, violates). The WOTS+-replacement's safety claim now has a model (SPEC.md S1). | agent |
| 2026-07-28 | `aa4d5d500549573d` | quint 0.32.0 | **37/37, eight models** — added `delivery.qnt`: a hostile carrier cannot lose funds (`keeps`), but discarding a bundle on a transient verdict does (`discards`, violates) — the `ViewIncomplete` regression as a permanent guard (SPEC.md S13). | agent |
| 2026-07-29 | `2f4a7bad4318b14b` | quint 0.32.0 | **FULL suite, 32/32, ~5.5 min, seven models** — after retiring `onetime.qnt` with the signature itself (proof-native auth has no key a second use discloses; its successor for the question that remains is `authorization.qnt`). `baserail.qnt` premises re-derived for proof-native auth, comments only: all 9 rows unchanged, because the model never leaned on signatures. | agent |
| 2026-07-29 | `4f5cb2e225a19ae9` | quint 0.32.0 | **FULL suite, 35/35 — three new all-depths proofs.** `authorization.enforced onlyOwnerSpends` inductive via `authInv` (typeOK + preimage knowledge never spreads, ~0.3 s); `issuance.strict` `supplyIsKnown` AND `assetSupplyIsKnown` inductive via its `supplyInv` (~100 s each — the strengthening needed `nextLink == idCounter`, whose absence Apalache demonstrated with a drifted-counters start state). Four inductive rows total with multihop's; every remaining bounded row unchanged. | agent |
| 2026-07-29 | `915fee9627edb8c2` | quint 0.32.0 | **37/37 — two more all-depths proofs.** `reorg.reconciled` `acceptedStaysValid` via `reorgInv` and `acceptedHasLiveGenesis` via `genesisInv`, ~4 s each: six inductive rows now. Also this session: `linkage.qnt` gained a strengthening (`linkInv`, with `uniqueIds`/`freshIds` to collapse `trulyDescends`' nested existentials) whose proof **the tool cannot do at this encoding**: 4 GB heap dies, 16 GB thrashed a 16 GB laptop, and a 64 GB heap on a 243 GB / 64-core box returned `table overflow` after 22 minutes — a Z3 internal limit, so more hardware is not the answer. Linkage stays a bounded row; the attempt, the cause (`trulyDescends`' nested existentials under an arbitrary start state) and the named next idea are written up in `formal/README.md` rather than hidden. Note for anyone retrying: quint hardcodes `-Xmx4096m` for the server it spawns, so a bigger heap needs `apalache.jar server` started by hand plus `--server-endpoint`. | agent |
| 2026-07-30 | `8708821171dec30c` | quint 0.32.0 | **FULL suite, 50/50 — the linkage induction closed, and six missing falsification pairs found.** `linkage.checked noSplicedHistoryG` is inductive via `linkInv` **in 15.8 s on the laptop that could not do it at all**, after the direct encoding beat a 4 GB heap, a 16 GB laptop and a 243 GB / 64-core box (`table overflow`, 22 min). The fix was the encoding, not the hardware: descent is carried in a **ghost variable** so the property is `accepted ⊆ descends`, and `trulyDescends`' four nested existentials never enter the induction. Seven inductive rows now. Guarded by four falsification rows — the ghost property pointed at the splice attack (violates), the induction pointed at `unchecked` (violates, so `linkInv` proves the protocol's rule and not the model's bookkeeping), `ghostFaithful` in both modules (the ghost tracks the real hop graph), and `onlyGenesisChildrenAccepted` (violates, so a 2-hop chain really is accepted and `linksUp` is exercised where it can differ from `allProven`). **Separately: a comment claiming every inductive row had such a pair turned out to be false — only linkage did.** Pairs added for the other six (`reorg shallow`/`genesisUnchecked`, `issuance byAmount`/`globalSum`, `authorization anchorOnly`, `multihop unbound`); all six fail as required. One model bug fixed on the way: `fabricateHop`'s fictional note drew from `MAX_HOP.to(MAX_HOP+2)`, whose low end collides with the last creatable note, so some "fabricated" hops were spending a real note and were not attacks. | agent |
| 2026-07-30 | `db8c20c8cd0c0796` | quint 0.32.0 | **FULL suite, 68/68 — both LIVENESS models now hold at all depths, the last two that did not.** `delivery.keeps noSettledPaymentLost` inductive via `deliveryInv` = *the payment is always either already taken or still re-sendable* (~4 s). The property alone is NOT inductive: from a state where only `bundleHeld` carries it, the **unguarded** `drop` breaks it — invisible to bounded checking because that state is unreachable under `keeps`. `baserail` gained four: `paymentRemainsPossible` via `railInv` (`atomic`, ~5 s) and `splitInv` (`splitPayment`, ~5 s), `nobodyElseGetsPaid` via `railSafetyInv` (`griefable`) and `splitSafetyInv` (`splitGriefed`) — safety proved **with the adversary on**, which is the only place worth proving it, since griefing destroys a payment without ever redirecting one. **12 inductive rows now, and every model has at least one.** Three encoding lessons, each found by a counterexample rather than reasoning: (1) four booleans still need a `typeOK` — the inductive invariant is the *initialization* predicate, so it must assign every variable, and Apalache said so precisely; (2) a domain bound must be **closed under the transition**, not merely large — bounding `nextId` to `1.to(3)` let the induction start at 3, fire `build`, and step to 4, and padding never fixes that (every K has a K+1) — what fixes it is relating the counter to what it counts; (3) a bundle with **empty inputs** is well-typed, intersects nothing, and so fails to block `build` — hence `bundlesAreFundingSets` in both invariants. `splitRecords` stays bounded and always will: with the one-live-bundle rule off, `build` never disables and mints a fresh id forever, so no `typeOK` exists — capping it was **rejected** because that module's job is to *exhibit* a violating run and bounding its space risks bounding away the run. Also this session: `check-refs.sh` gained a **per-name inductive pairing check**, which immediately reported `railSafetyInv`/`splitSafetyInv` as UNPAIRED — the totals had read 12 `ok` / 12 `violation`, and one invariant having three partners exactly masked two having none. **A matching count is not a matching set.** Closed with `PRUNE_SETTLED`, which switches off the retention rule that both safety proofs rest on, labelled model-soundness rather than protocol because no adversary constant in this file can break safety. | agent |
| 2026-07-30 | `908f716f5eac6ed5` | quint 0.32.0 | **FULL suite, 68/68, no regressions** — a re-run after the legacy-architecture prune, which touched three model headers and so moved the subject hash without touching a single rule. Recorded because that is exactly the case the subject hash exists for: the previous row's hash no longer describes these files, and a reader who assumed it did would be trusting a result about text that has changed. `authorization.qnt` said the money path *is moving off* signatures (it moved a day earlier), `linkage.qnt` explained itself by describing the deleted design it came from, and `baserail.qnt` justified a technique by pointing at a model that no longer exists. All three now state the current rule and cite `formal/README.md` for the methodology. No property, module, invariant or row changed; 68 checks, 12 inductive, every one still paired. | agent |
| 2026-07-30 | `73a02e7aba879c13` | quint 0.32.0 | **FULL suite, 68/68, no regressions** — second re-run of the day, after repointing every citation to a specification chapter that no longer exists. Thirteen numbered chapters were merged into `SPEC.md` months ago and **31 citations across 20 files still named them**, four of those inside `formal/` (`issuance.qnt` ×3, `verify.sh` ×2), which is what moved this hash. `check-refs.sh` could not see them: it validates *source paths* cited by models, and these were *document* references — a check running in one direction again. All 36 anchor links into `SPEC.md` now resolve, verified mechanically. Also landed: `formal/COMPOSITION.md` gained **A7, Keccak**, taking layer-0 from 6 assumptions to 7 without a line of code changing — the proof system commits with Keccak (trace Merkle tree, FRI folding, Fiat–Shamir challenger) and no document named it, while A2 rested on it. Found because someone asked whether Keccak was legacy. No property, module, invariant or row changed; 12 inductive, every one still paired. | agent |
| 2026-07-31 | `b65cf27320d9a985` | quint 0.32.0 | **70 checks — recursion gets a safety gate.** `reorg.qnt` gained a `recursion` module: a root-freshness abstraction (one fork-height variable, no accumulator tree) and the liveness property `paymentRemainsPossible`, in baserail's shape. Two rows, a matched pair. `recursionReprovable` (kept the ancestry, or the walk) HOLDS — the control. `recursionStrict` (pure recursion) VIOLATES in **3 steps**: mine to height 1, receive a coin whose recursive proof commits to height 1, reorg to height 1 — the proof's root is rewritten and the coin is stranded, valid and unspendable. This is [ACC] Part 2's safety gate, and it fails the naive design; recorded there with the three non-free doors. The finding cost an afternoon and a formal model, ahead of any FRI-verifier cost measurement. | agent |
| 2026-07-31 | `f3bf90643a7447af` | quint 0.32.0 | **73 checks — the first observer model, plus a measured ceiling.** `formal/privacy.qnt` (the eighth model) asks what a WATCHER learns, not a thief: an on-chain record event and an addressed off-chain delivery compose. `lone` VIOLATES `unlinkable` — record + delivery in one window pairs the payer->payee edge, amount sealed; `offband` and `decorrelated` HOLD (the two mitigations). Payment-graph privacy is traffic and timing, not cryptography — P4 in the matrix, layer 4 (orthogonal) in COMPOSITION. Also: `multihop`'s supply-conservation ceiling MEASURED — inductive at 5 notes (57s), bounded-holds at 7 (no inflation), inductive strengthening breaks past 5 without re-tuning, prover unfinished at 8 in 5 min. The convenient 'all depths over a 5-note universe' is now a measured number with its edge named. | agent |
